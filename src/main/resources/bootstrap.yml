spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: file:///${user.home}/private/formation/master-microservices-spring-boot-spring-cloud/codes/microservices/git-localconfig-repo
server:
  port: 8888